"use client";
import {useMemo} from "react"; import {usePathname,useRouter,useSearchParams} from "next/navigation"; import Link from "next/link"; import {formatDate} from "@/src/lib/date";
type Item={slug:string;title:string;subtitle:string;description:string;publishedAt:string;category:string;topics:string[];keywords:string[];readingTimeMinutes:number;hero:{src:string;alt:string}};
export function ArchiveFilters({items}:{items:Item[]}){const params=useSearchParams(),router=useRouter(),path=usePathname();const q=params.get("q")||"",category=params.get("category")||"",year=params.get("year")||"",topic=params.get("topic")||"";const set=(key:string,value:string)=>{const p=new URLSearchParams(params);value?p.set(key,value):p.delete(key);router.replace(`${path}?${p}`)};const categories=[...new Set(items.map(i=>i.category))],years=[...new Set(items.map(i=>i.publishedAt.slice(0,4)))],topics=[...new Set(items.flatMap(i=>i.topics))].sort();const filtered=useMemo(()=>items.filter(i=>{const hay=[i.title,i.subtitle,i.description,i.category,...i.topics,...i.keywords].join(" ").toLowerCase();return (!q||hay.includes(q.toLowerCase()))&&(!category||i.category===category)&&(!year||i.publishedAt.startsWith(year))&&(!topic||i.topics.includes(topic))}),[items,q,category,year,topic]);return <><div className="archive-controls"><label>Search<input type="search" value={q} onChange={e=>set("q",e.target.value)} placeholder="Search the archive"/></label><label>Category<select value={category} onChange={e=>set("category",e.target.value)}><option value="">All categories</option>{categories.map(x=><option key={x}>{x}</option>)}</select></label><label>Year<select value={year} onChange={e=>set("year",e.target.value)}><option value="">All years</option>{years.map(x=><option key={x}>{x}</option>)}</select></label><label>Topic<select value={topic} onChange={e=>set("topic",e.target.value)}><option value="">All topics</option>{topics.map(x=><option key={x}>{x}</option>)}</select></label></div><p className="result-count" aria-live="polite">{filtered.length} {filtered.length===1?"publication":"publications"}</p>{filtered.map(i=><Link className="research-card" href={`/research/${i.slug}`} key={i.slug}><img className="research-card-thumb" src={i.hero.src} alt="" aria-hidden="true"/><div className="research-card-copy"><div className="research-card-kicker"><span className="meta">{formatDate(i.publishedAt)}</span><span className="label">{i.category}</span></div><h3>{i.title}</h3><p>{i.subtitle}</p><div className="tags">{i.topics.map(t=><span className="tag" key={t}>{t}</span>)}</div></div><span className="research-card-read">{i.readingTimeMinutes} min →</span></Link>)}{!filtered.length&&<p className="empty">No published research matches these filters.</p>}</>}
