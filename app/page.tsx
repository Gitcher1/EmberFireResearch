import Link from "next/link"; import {getAllResearch,getFeatured,formatDate} from "@/src/lib/research";
export default function Home(){const featured=getFeatured();const all=getAllResearch();if(!featured)throw new Error("No featured publication");return <><section className="hero"><div className="eyebrow">EmberFire Research</div><p className="hero-line">Investigating the gaps between the official record and the full story.</p><div className="hero-grid"><div><div className="eyebrow">Featured investigation</div><p className="meta">{featured.meta.category} · {formatDate(featured.meta.publishedAt)}</p><h1>{featured.meta.title}</h1><p className="dek">{featured.meta.subtitle}</p><p>{featured.meta.readingTimeMinutes} minute read</p><div className="button-row"><Link className="button" href={`/research/${featured.meta.slug}`}>Read the investigation</Link><Link href="/archive">Research archive →</Link></div></div><figure><img src={featured.hero.src} alt={featured.hero.alt}/><figcaption>{featured.hero.caption} <span className="credit">Credit: <a href={featured.hero.source}>{featured.hero.credit}</a> · {featured.hero.license}</span></figcaption></figure></div></section><section className="section"><p className="statement">EmberFire Research publishes source-driven investigations that follow questions through documents, chronology, competing interpretations, and unresolved evidence.</p></section><section className="section"><h2 className="section-title">Latest Research</h2>{all.map(p=><Link className="research-card" key={p.meta.slug} href={`/research/${p.meta.slug}`}><img className="research-card-thumb" src={p.hero.src} alt="" aria-hidden="true"/><div className="research-card-copy"><div className="research-card-kicker"><span className="meta">{formatDate(p.meta.publishedAt)}</span><span className="label">{p.meta.category}</span></div><h3>{p.meta.title}</h3><p>{p.meta.subtitle}</p><div className="tags">{p.meta.topics.map(t=><span className="tag" key={t}>{t}</span>)}</div></div><span className="research-card-read">{p.meta.readingTimeMinutes} min →</span></Link>)}<div className="support-grid"><Link className="support-card" href="/standards"><span className="label">Our practice</span><h3>Research standards</h3><p>How claims, sources, uncertainty, versions, and corrections are handled.</p></Link><Link className="support-card" href="/archive"><span className="label">Public record</span><h3>Research archive</h3><p>Browse every published investigation by date, category, and topic.</p></Link><Link className="support-card" href="/about"><span className="label">The publication</span><h3>About EmberFire Research</h3><p>Why we reconstruct the larger record behind compressed public stories.</p></Link></div></section></>}
